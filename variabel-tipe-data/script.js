var x = 12 // Deklarasi x sebagai variabel, dan 12 sebagai nilai atau isi dari variabel js dengan tipe data number(integer)
let y = 'testing' // Deklarasi y sebagai variabel, dan testing sebagai nilai atau isi dari variabel js dengan tipe data string
const z = true // Deklarasi z sebagai variabel konstan(tidak dapat ditimpa), dan true sebagai nilai atau isi dari variabel js dengan tipe data boolean

console.log(x + y + z) //console.log untuk menampilkan variabel di console
